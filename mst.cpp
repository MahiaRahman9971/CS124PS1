#include <vector>
#include <iostream>
using namespace std;

std::vector<int> distance; 
std::vector<int> prev;

void Prims(std::vector<std::vector<double>> vertices) {
    int n = vertices.size();

    // int d[n] = {0};
    // int S[n] = {0};
    // int Prev[n] = {0};
    // int H[n] = {0};
}

// 